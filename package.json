{
  "name": "viva-bem",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass:watch": "sass src/scss:src/css --watch --style=expanded --embed-source-map --quiet-deps",
    "serve": "browser-sync start --server --files \"index.html, src/css/*.css\" --no-notify --port 3000 --open",
    "dev": "run-p sass:watch serve",
    "build": "sass src/scss/styles.scss src/css/styles.css --style=compressed --no-source-map"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "npm-run-all": "^4.1.5",
    "sass": "^1.94.0"
  }
}
